query PokemonDB{
    pokemons(first: 20) {
        name
        number
    }
}

query PokemonByName ($name: String){
    pokemon(name: $name) {
        name
        image
        weight{
            minimum
        }
        height{
            minimum
        }
    }
}